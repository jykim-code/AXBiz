# AX Biz Radar — 관리자 운영 가이드

> 보고서 데이터를 **넣고 → 검수하고 → 발행**하는 방법. (비개발자 관리자용)
> 코드/배포 같은 개발 작업은 이 문서 범위가 아닙니다.

---

## 0. 핵심 개념 — 화면이 두 개입니다

| 화면 | 주소 | 무엇 |
|---|---|---|
| **운영(Published)** | `ax-biz-radar.pages.dev` | **일반 사용자가 보는 실서비스.** 발행된 데이터만. |
| **검수 미리보기(Staging)** | `stg.ax-biz-radar.pages.dev/preview` | **관리자 전용.** 아직 발행 안 된 **draft(초안)** 데이터까지 합쳐서 "발행하면 이렇게 보인다"를 미리 확인. |

> 핵심 원칙: **관리자가 넣은 데이터는 곧바로 공개되지 않습니다.** `draft(초안)`으로 쌓였다가, **검수 후 "배포" 버튼**을 눌러야 운영(Published)에 반영됩니다.

전체 흐름:
```
데이터 넣기(가져오기/수동)  →  draft 적재  →  검수(수정·삭제)  →  미리보기로 확인  →  배포  →  실서비스 공개
```

---

## 1. 접속

1. `ax-biz-radar.pages.dev/admin` 접속 → **관리자 PIN** 입력.
2. 관리자 페이지 탭: **가져오기 · 수동 입력 · 검수·배포 · DART 연결 · 의견함 · 위클리 픽 · 태그 관리**

---

## 2. 데이터 넣기 (둘 중 택1) — 모두 draft로 들어갑니다

### A. 컨플루언스에서 가져오기 — **[가져오기] 탭**
1. **데일리 / 히스토리** 중 선택
   - **데일리**: "AX 플랫폼 주요 경쟁사 사업동향 [날짜]" 페이지 → 섹션 A(상위 보고용)를 AI가 기업별로 자동 정리. (한 날짜 × 여러 기업)
   - **히스토리**: "AX 동향 히스토리 - 기업(연도)" 표 페이지 → 표를 그대로 가져옴. (한 기업 × 여러 날짜)
2. 컨플 페이지 **URL 붙여넣기** → **[draft로 가져오기]**
3. 자동으로 **검수·배포 탭**으로 이동합니다.

### B. 직접 입력 — **[수동 입력] 탭**
1. 날짜 선택 → **[+ 기업 추가]** → 기업명·분류·주요내용·시사점·한컴 인사이트·태그 입력
2. (기존 항목 수정 시) **[기존 데이터 불러오기]**로 불러와 수정
3. **[draft로 저장 → 검수]** → 검수·배포 탭으로 이동

> 둘 다 **라이브(실서비스)에는 아직 반영되지 않습니다.** draft로만 쌓입니다.

---

## 3. 검수 — **[검수·배포] 탭**

draft 항목이 날짜별로 표시됩니다. 각 항목에 **상태 배지**가 붙습니다:

| 배지 | 뜻 | 어떻게 |
|---|---|---|
| 🟢 **신규** | 실서비스에 없는 새 항목 | 확인 후 **배포**(추가됨) |
| 🟠 **교체** | 실서비스에 있는데 내용이 다름 | 필요 시 **수정** 후 **배포**(덮어씀) |
| ⚪ **동일** | 실서비스와 내용이 같음(바뀐 것 없음) | 배포 불필요 → **삭제**로 정리 (`동일 N건 정리` 버튼) |

- **편집**: 항목의 요약·주요내용·시사점·한컴 인사이트·태그·분류 수정 → 저장
- **삭제**: 그 draft만 제거(실서비스 영향 없음)
- **`DART 미연결` 배지**가 보이면 → 처음 등장한 기업입니다(아래 6번 참고)

---

## 4. 미리보기 — **[검수 미리보기 ↗] 버튼** (상단 우측)

1. 버튼 클릭 → `stg.ax-biz-radar.pages.dev/preview` 새 탭 → **PIN 한 번 더 입력**
   - (운영과 주소가 달라 PIN을 다시 받습니다. 정상입니다.)
2. 실서비스와 똑같은 화면이 뜨는데, **상단에 검은 "DEV 프리뷰" 배너**가 있고 **draft 데이터까지 합쳐서** 보입니다.
3. 사이드바로 **검색·기업** 페이지를 눌러 이동해도 미리보기가 유지됩니다(배너로 확인).
4. 빠져나오려면 배너의 **"실서비스 보기"**.

> 미리보기에서 **반영되지 않는 것**(의도된 동작):
> - **검색의 자연어(AI) 질문** — 발행된 데이터만 검색합니다(초안 미반영).
> - **기업 상세의 AI 요약 밴드** — 발행 후 생성됩니다.
> - 그 외 카드·그래프·기업 타임라인은 draft가 모두 보입니다.

---

## 5. 배포 (발행) — **[검수·배포] 탭 또는 미리보기 배너**

- **건별**: 항목의 **[이 건 배포]**
- **날짜별**: **[이 날짜 배포]**
- **전체**: **[전체 배포 ▶]**

배포하면 **즉시 실서비스(Published)에 반영**되고, 같은 (날짜·기업) 항목은 **교체(덮어쓰기)**됩니다. (확인 창이 한 번 뜹니다.)

---

## 6. DART 연결 (재무 표시) — **[DART 연결] 탭**

- 기업 상세 페이지의 **회사정보·재무**는 DART 법인 매핑이 있어야 표시됩니다.
- **이미 연결된 기업**(네이버·삼성SDS 등): 아무것도 안 해도 자동 표시.
- **처음 등장한 기업**(검수에서 `DART 미연결` 배지): DART 연결 탭에서 **이름으로 검색 → 선택**해 1회 매핑하면 이후 자동.
- 해외/비상장 등 DART 대상이 아니면 매핑 안 해도 됩니다(재무만 미표시).

---

## 7. 위클리 픽 발행 — **[위클리 픽] 탭**

주 1회 공유할 **발행물**을 만드는 곳입니다. 대시보드와 달리 **그 주에서 주목할 것만 골라** 회차 번호를 붙여 발행합니다.

**언제**: 그 주 마지막 검수를 끝낸 뒤(권장: 금요일 17:30 배포 직후). 5~10분 걸립니다.

1. **주차 선택** — 그 주의 아무 날짜를 고르고 **[불러오기]**. 그 주 동향 전체와 수치가 나옵니다.
2. **주목할 것 체크 (건별)** — 목록은 「신호」 순으로 정렬됩니다. 신호는 네 가지(신규 기업 · 새 영역 · 공통 주제 · 신규 주제)이고
   **정렬을 돕는 것일 뿐 고르는 것은 사람**입니다. **수량 제한은 없습니다** — 그 주에 주목할 것이 많으면 많이 실으세요.
   선택은 **기업별이 아니라 건별**이라 같은 기업의 동향이 그 주에 둘이면 두 줄로 뜨고 따로 고릅니다.
3. **각 건에 「주목(Pick) 이유」 한 줄** (60~140자) — **이것이 이 페이지의 알맹이입니다.**
   대시보드에 없는 유일한 정보입니다. **비어 있으면 발행되지 않습니다.** 막히면 **[AI 초안]**으로 초안을 받아 고쳐 쓰세요.
   - 제목도 고칠 수 있습니다. 원본 요약이 「~했습니다」처럼 서술형이면 이때 명사형으로 다듬으세요.
4. **이미지 (선택)** — 안 올려도 됩니다. 안 올리면 활자·색으로 된 판이 자동으로 들어갑니다.
   - **[파일 선택]** → 고르는 즉시 올라가고 미리보기가 뜹니다.
   - **출처·권리 근거는 필수**입니다(예: `한컴 제공`, `과기정통부 보도자료(공공누리 1유형)`, `직접 작성`).
     비우면 발행이 막힙니다. 반년 뒤에 이 사진을 어디서 가져왔는지 아무도 모르는 상태를 만들지 않기 위한 것입니다.
     **화면에는 표기되지 않습니다** — 기록으로만 남습니다.
   - 실리는 방식이 두 화면에서 다릅니다.
     **상세 페이지는 원본 비율·크기 그대로** 들어갑니다(잘리지 않습니다).
     **뉴스레터 슬라이드는 판 높이가 고정**이라 잘리므로, **잘리는 기준**(위·가운데·아래)으로 무엇을 남길지 정합니다.
     관리자 미리보기는 슬라이드 판을 줄인 것이라 **여기서 잘려 보이는 대로 슬라이드에서 잘립니다.**
   - 1,240px보다 작으면 붉게 경고합니다. 화면에서 뭉개집니다.
   - ⚠ **기사 사진을 내려받아 올리지 마세요.** 링크가 아니라 복제라 위험이 더 큽니다. 기업 공식·공공누리·직접 만든 도표를 쓰세요.
5. **금주 한 줄 요약 · 한컴 관점** — 각각 **[AI 초안]** 후 다듬습니다. 결론은 **한 줄 = 불릿 1개**로 2~3줄.
   이 둘이 페이지 **최상단**에 옵니다. 링크로 들어온 사람이 스크롤 없이 읽는 부분이라 여기에 가장 공을 들이세요.
6. **[미리보기 ↗]** — 상세 페이지를 발행 전에 확인합니다(아직 공개되지 않음).
   **[뉴스레터 미리보기 ↗]** — 같은 초안을 한 장씩 넘겨 보는 화면으로 확인합니다. 올린 이미지가 여기서 사진 판으로 보입니다.
7. **[발행]** → 회차 번호가 붙고 `ax-biz-radar.pages.dev/weekly?w=…` 주소로 공개됩니다.
8. **메시지 발송은 버튼이 둘이고, 순서가 정해져 있습니다.**

   | 버튼 | 가는 곳 | 되돌릴 수 있나 |
   |---|---|---|
   | **[1차 리허설 발송]** | AX멘토링(테스트) | 실수해도 전사에 안 보입니다 |
   | **[2차 전사 발송]** (빨간 테두리) | **전사 라운지** | **되돌릴 수 없습니다** |

   - **1차를 먼저 누르고, 그 방에서 눈으로 확인한 뒤 2차를 누릅니다.** 1차를 건너뛰고 2차를 누르면
     「1차 리허설 발송을 먼저 해 주세요」로 막힙니다.
   - **1차와 2차의 문구는 같습니다.** 확인 창이 이것을 글자 지문으로 보여 줍니다 —
     `✅ 1차 리허설 08-24 10:12 → AX멘토링(테스트) / 그때 보신 문구와 지금 문구가 같습니다`.
     1차 뒤에 회차 내용을 고쳤으면 2차가 막히고, 1차부터 다시 하게 합니다.
   - 두 버튼 모두 **나갈 문구를 그대로 보여주고 확인**을 받습니다. 취소하면 아무것도 나가지 않습니다.
   - 문구는 자동으로 만들어집니다(주차 제목 · 수치 · 픽 제목 목록 · 상세 페이지 링크). **쓸 것이 없습니다.**
   - 이미 전사로 보낸 회차면 **보낸 시각을 보여주고 한 번 더 묻습니다.**
   - ⚠ **메시지 옆 아이콘·이름은 이 화면이 정하지 않습니다.** 방마다 웹훅을 만들 때 각각 설정한 값이라
     1차에서 잘 보이는 것이 전사 라운지를 보장하지 않습니다. 두 방의 웹훅 설정을 한 번 대조해 두세요
     (Chat 스페이스 → 앱 및 통합 → 웹훅). 매주 확인할 일은 아닙니다.
   - 「웹훅 주소가 설정되지 않았습니다」가 나오면 관리자가 Cloudflare에 주소를 넣고 **재배포**해야 합니다(아래 FAQ).

> **발행하면 그 시점 내용이 고정됩니다.** 나중에 원본 데이터를 고쳐도 이미 공유한 링크의 내용은 바뀌지 않습니다.
> 발행본을 갱신하려면 내용을 고치고 **[다시 발행]**을 누르세요.
> 잘못 발행했으면 **[발행 회수]** — 단, 이미 공유한 링크는 빈 화면이 됩니다.

---

## 8. 자주 묻는 것 (FAQ)

- **Q. 데이터를 넣었는데 실서비스에 안 보여요.**
  → 정상입니다. draft 상태입니다. **검수·배포 탭에서 "배포"**를 눌러야 공개됩니다.
- **Q. 미리보기에서 홈만 보이고 검색/기업은 실서비스로 빠져요.**
  → 옛 현상입니다. 지금은 미리보기가 탭 단위로 유지됩니다. 안 되면 미리보기 화면에서 **새로고침(Ctrl+Shift+R)** 한 번.
- **Q. 미리보기 들어갈 때 PIN을 또 물어봐요.**
  → 주소(도메인)가 운영과 달라서입니다. 한 번 입력하면 그 탭/브라우저에서 유지됩니다.
- **Q. 같은 컨플 페이지를 또 가져오면?**
  → 같은 항목은 갱신됩니다(중복 안 쌓임). 안 바뀐 항목은 **동일** 배지 → 삭제로 정리.
- **Q. 의견함은?**
  → [의견함] 탭에서 사용자가 보낸 의견(기업 추가 요청·오류 제보 등)을 확인합니다.
- **Q. [메시지 보내기]를 눌렀더니 「웹훅 주소가 설정되지 않았습니다」가 나옵니다.**
  → Cloudflare 대시보드 **Pages → ax-biz-radar → Settings → Environment variables** 에
  `WEEKLY_WEBHOOK_URL`(메신저 웹훅 주소)을 넣어야 합니다. 두 가지를 같이 확인하세요.
  ① **환경이 나뉩니다** — 운영은 *Production*, stg는 *Preview*라 **양쪽에 각각** 넣어야 합니다.
  ② **넣은 뒤 재배포해야 합니다** — 환경변수는 배포 시점에 묶여서, 값만 넣고 재배포하지 않으면 그대로 안 보입니다.
  → 확인 방법: 관리자로 로그인한 브라우저에서 `/api/health` 를 열면 `secrets.WEEKLY_WEBHOOK_URL` 이
  `true`/`false`로 나옵니다(**값은 안 보이고 설정 여부만** 나옵니다). `false`면 위 ①②를 다시 보세요.
- **Q. 링크가 stg 주소로 나갑니다.**
  → `SITE_ORIGIN` 을 넣지 않으면 요청 도메인이 그대로 링크가 됩니다. 운영 환경에는 반드시
  `https://ax-biz-radar.pages.dev` 를 넣으세요. stg에서는 일부러 비워 두면 시험용임이 드러나서 안전합니다.
- **Q. 메시지가 어느 방으로 갑니까?**
  → **누른 버튼**이 정합니다. [1차 리허설 발송]=테스트 방, [2차 전사 발송]=전사 라운지.
  누르는 화면의 도메인은 **주소 묶음**을 정합니다.

  | 누르는 곳 | 읽는 환경 | 1차 | 2차 |
  |---|---|---|---|
  | `stg.ax-biz-radar.pages.dev/admin` | Preview | AX멘토링(테스트) | AX멘토링(테스트) |
  | `ax-biz-radar.pages.dev/admin` | Production | AX멘토링(테스트) | **전사 라운지** |

  stg 는 1차·2차 모두 테스트 방으로 가게 두었습니다 — 화면 조작 순서를 안전하게 연습할 수 있습니다.
  두 화면은 **생김새가 같고 같은 데이터를 봅니다**(D1 을 공유). 그래서 확인 창 맨 위에
  **보낼 곳**을 적어 둡니다 — 「보낼 곳: 전사 라운지」가 보이면 전사에 나갑니다.
  이 줄 대신 「⚠ 보낼 곳을 확인할 수 없습니다」가 나오면 이름표(`WEEKLY_WEBHOOK_LABEL` ·
  `WEEKLY_WEBHOOK_TEST_LABEL`)가 비어 있는 것이니 **누르지 말고** 먼저 채우세요.

- **Q. 전사에 잘못 보냈습니다. 회수됩니까?**
  → **웹훅으로 보낸 메시지를 이 화면에서 되돌리는 기능은 없습니다.** 지우려면 Chat 에서
  전사 라운지 스페이스 관리자 권한으로 해당 메시지를 삭제해야 합니다(메시지 우측 ⋮ → 삭제).
  그래서 **[2차 전사 발송] 전에 반드시 [1차 리허설 발송]으로 확인**하는 순서를 만들어 두었습니다.

- **Q. 환경변수가 이제 몇 개입니까?**
  → 발송 관련 4개입니다. **Production 과 Preview 양쪽에** 넣습니다.

  | 이름 | Production 값 | Preview(stg) 값 |
  |---|---|---|
  | `WEEKLY_WEBHOOK_URL` | 전사 라운지 웹훅 | AX멘토링 웹훅 |
  | `WEEKLY_WEBHOOK_LABEL` | `전사 라운지` | `AX멘토링(테스트)` |
  | `WEEKLY_WEBHOOK_TEST_URL` | AX멘토링 웹훅 | AX멘토링 웹훅 |
  | `WEEKLY_WEBHOOK_TEST_LABEL` | `AX멘토링(테스트)` | `AX멘토링(테스트)` |

---

## 9. 한 줄 요약

> **/admin에서 데이터 넣기 → 검수 → "검수 미리보기 ↗"로 확인 → "배포"** = 끝.
> 관리자가 외울 주소는 **`/admin`** 하나, 확인은 **"검수 미리보기"** 버튼 하나입니다.
