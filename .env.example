# wrangler CLI 인증용 (이 파일을 복사해 `.env` 로 저장 후 값 채우기 — .env 는 커밋 금지)
#   - Cloudflare 대시보드 > My Profile > API Tokens > Create Token
#   - "Edit Cloudflare Workers" 템플릿 + D1:Edit, Cloudflare Pages:Edit 권한 추가
# 계정이 여러 개면 CLOUDFLARE_ACCOUNT_ID 도 함께 설정.
CLOUDFLARE_API_TOKEN=
# CLOUDFLARE_ACCOUNT_ID=
