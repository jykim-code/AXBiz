# 신규 협업자 셋업 가이드 (AX Biz Radar)

> VSCode + Claude Code로 이 프로젝트를 함께 개발하는 분을 위한 시작 안내.
> **개발은 Claude Code에게 한국어로 요청**하면 됩니다. git/배포 같은 건 Claude Code가 대신 처리합니다.

---

## 0. 큰 그림 (꼭 이해하기)

```
내 브랜치에서 작업  →  PR(풀리퀘스트) 올림  →  스테이징 URL 자동 갱신  →  관리자 검토  →  관리자가 머지  →  운영 사이트 반영
```

- **나는 운영(실서비스)에 직접 배포하지 않습니다.** 항상 내 **브랜치 → PR**까지만. 운영 반영은 **관리자가 검토 후 머지**할 때만 일어납니다.
- `main`(운영 브랜치)에는 직접 못 올립니다(잠겨 있음). 실수해도 안전합니다.

---

## 1. 처음 한 번만 (셋업)

### 1-1. GitHub 저장소 접근
- 관리자가 보낸 **저장소 초대(Invitation)를 수락**하세요. (GitHub 이메일 또는 `github.com/jykim-code/AXBiz` 접속 시 수락 버튼)

### 1-2. VSCode에 저장소 받기 (clone)
1. VSCode 좌측 **소스 제어(Source Control)** 아이콘 → **"Clone Repository"**
2. `https://github.com/jykim-code/AXBiz.git` 입력
3. **GitHub 로그인 창**이 뜨면 로그인(브라우저로 인증). 저장 위치 선택 → 열기
   - (또는 터미널: `git clone https://github.com/jykim-code/AXBiz.git`)

### 1-3. 도구 확인
- **VSCode + Claude Code**: 이미 설치하셨다면 OK.
- **Node.js**: 설치돼 있으면 좋지만, **로컬 실행은 보통 안 해도 됩니다**(아래 참고). 없으면 일단 넘어가세요.

> 💡 **로컬에서 앱을 띄울 필요는 거의 없습니다.** 우리 앱은 클라우드(DB·AI)에 의존해서, 변경은 **PR 올리면 자동 생성되는 프리뷰 URL**에서 실제로 확인하는 게 정확합니다. (그래서 비밀키·토큰을 내 노트북에 둘 필요도 없습니다.)

---

## 2. 매번 작업할 때 (Claude Code에게 이렇게)

작업은 **Claude Code 채팅에 한국어로 요청**하면 됩니다. 핵심은 **"새 브랜치에서 작업하고 PR까지 올려줘"**라고 말하는 것:

> 예시 요청
> - *"새 브랜치 만들어서, 대시보드 상단 검색창 색을 좀 더 진하게 바꾸고, 끝나면 PR 올려줘."*
> - *"`기업 카드`의 날짜 표시 위치를 오른쪽으로 옮기고 싶어. 브랜치 따서 작업하고 PR 만들어줘."*

그러면 Claude Code가 알아서:
1. **새 브랜치 생성** (예: `feature/search-color`)
2. 코드 수정
3. 커밋 & **푸시**
4. **PR(풀리퀘스트) 생성**

까지 해줍니다. (git 명령을 직접 외울 필요 없음 — 막히면 *"지금 어떤 브랜치야?"*, *"PR 올려줘"* 처럼 물어보세요.)

> ⚠️ Claude Code에게 **"main에 바로 배포해줘" / "운영에 올려줘"라고 하지 마세요.** 그건 관리자 역할입니다. (시도해도 GitHub이 막습니다.)

---

## 3. 내 변경이 어디로 가나 (PR → 스테이징 → 검토 → 운영)

PR(또는 브랜치 푸시)을 올리면:
1. 내 변경이 **단일 스테이징 페이지**에 반영됩니다 — 주소는 항상 하나: **`https://stg.ax-biz-radar.pages.dev`** (PR마다 새 주소가 생기지 않음)
   - 여기서 **내 변경이 실제로 어떻게 보이는지** 확인하세요.
   - 데이터(초안)까지 검수하려면 **`https://stg.ax-biz-radar.pages.dev/preview`** (관리자 PIN 필요).
   - (PR에는 봇이 이 스테이징 링크를 댓글로 달아줍니다.)
2. **문법 검사**가 자동으로 돌아 깨진 코드면 빨간 X가 뜹니다(고치면 됩니다).
3. **관리자가 PR을 검토** → 코멘트로 수정 요청하거나 **승인 후 머지**.
4. 머지되면 **운영 사이트(`ax-biz-radar.pages.dev`)에 자동 반영**됩니다.

> 수정 요청을 받으면? 같은 브랜치에서 Claude Code에게 *"리뷰 코멘트대로 고쳐줘"*라고 하면, 그 PR이 자동으로 갱신되고 스테이징도 다시 빌드됩니다.

---

## 4. 데이터 vs 코드 (중요)

- **보고서/동향 같은 "데이터"를 바꾸는 건 개발(코드)이 아닙니다.** 그건 `/admin` 페이지에서 가져오기/수동 입력 → 검수 → 배포로 합니다. (PR 불필요 — `ADMIN-GUIDE.md` 참고)
- 위의 **PR 흐름은 "기능·화면(코드)"을 바꿀 때만** 쓰세요.

---

## 5. 하면 안 되는 것 (안전 규칙)

- ❌ `main` 브랜치 직접 수정/푸시 (막혀 있음)
- ❌ 운영(production) 직접 배포 요청
- ❌ 비밀키·PIN·토큰을 코드/커밋에 넣기 (`.env`, `.dev.vars`는 커밋 금지 — 이미 차단돼 있음)
- ✅ 항상 **새 브랜치 → PR**, 운영 반영은 관리자에게 맡기기

---

## 6. 막힐 때

- **Claude Code에게 그대로 물어보세요**: *"방금 한 거 PR로 어떻게 올려?"*, *"내 브랜치 이름 뭐야?"*, *"이 에러 무슨 뜻이야?"*
- **프로젝트 규칙**은 `CLAUDE.md`(개발 지침), `ROADMAP.md`(할 일), `ADMIN-GUIDE.md`(운영)에 있습니다. Claude Code가 자동으로 참고합니다.
- 그래도 막히면 **관리자에게 PR 링크와 함께 문의**.

---

## 한 줄 요약
> **clone → Claude Code에 "새 브랜치 만들어 작업하고 PR 올려줘" → PR 스테이징 확인 → 관리자 머지 = 운영 반영.**
> 나는 PR까지만, 운영 배포는 관리자가.
