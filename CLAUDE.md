# AX Biz Radar — 개발 지침 (사용자 명시 규칙)

> 이 파일은 사용자가 명시한 지침을 누적 기록하는 단일 출처(Source of Truth)다.
> Claude는 매 작업 전 이 파일을 먼저 확인하고, 새 지침이 나오면 여기에 추가한다.
> 디자인 관련 지침은 항상 `design-preview.html`을 기준으로 한다.

## 📁 문서 역할 지도 (2026-08-13 정리)

> 지시가 들어오면 **아래 표에서 담당 문서를 먼저 정한 뒤** 그 문서를 고친다.
> 한 사안을 두 문서에 나눠 적지 않는다. 다른 문서에서 필요하면 참조 한 줄만 남긴다.

| 문서 | 역할 (한 줄) | 누가 읽는가 |
|---|---|---|
| **`CLAUDE.md`** (이 파일) | 사용자가 정한 **규칙**의 단일 출처. 디자인·배포·협업·데이터 문체 기준 + 지침 로그 | Claude(매 작업 전), 개발자 |
| **`프롬프트.MD`** | 컨플 회차 페이지 **작성 지침**. 추적 기준·편입 트리거·정보 소스·섹션 A 양식·문체·제출 전 점검 | 회차 작성자, 클로드 앱 |
| **`동료 가이드.md`** | **하루 운영 절차**. 10:00 작성 / 17:30 갱신 / 그 직후 가져오기·검수·배포 | 회차 작성 담당자 |
| **`ADMIN-GUIDE.md`** | 관리자 화면 **조작법**(비개발자용). 가져오기·검수·배포 버튼 순서 | 관리자 |
| **`TODO.md`** | **앞으로 할 일**. 1순위 요구사항 + 주제별 백로그 | 개발자, Claude |
| **`ROADMAP.md`** | **완료 이력**. 끝난 작업만 완료일과 함께 | 개발자, 이력 확인 |
| **`ARCHITECTURE.md`** | **기술 구조 설명서**. 저장 구조·LLM 호출 지점·캐시·RAG 흐름·비용·제약 | 상사 보고, 신규 인원, Claude |
| **`LLM-PROVIDER-GUIDE.md`** | 키·모델·게이트웨이 **교체 실행 절차** (로컬 참고용, 커밋 대상 아님) | 개발자 |
| **`ONBOARDING.md`** | 신규 협업자 **셋업**과 브랜치→PR 규칙 | 신규 협업자 |
| **`README.md`** | 프로젝트 첫 소개, 파일 구조, 기술 스택 | 처음 보는 사람 |
| **`AX-Biz-Radar-기획.md`** | 초기 기획 **기록**(디자인 서술은 폐기). 현행 기준은 `CLAUDE.md`·`design-preview.html` | 배경 확인용 |
| **`HANDOFF.md`** | 세션 인계 메모. 2026-06-04에서 멈춰 있어 **현행과 어긋남**, 갱신 또는 폐지 판단 필요 | — |
| `design-preview.html` | 확정 디자인 기준(코드) | 프론트 작업 |
| `schema.sql` · `wrangler.toml` | D1 스키마·바인딩 기준(코드) | 백엔드 작업 |

**지시 라우팅** — 이런 말이 나오면 이 문서를 고친다

| 사용자 발화 예 | 담당 문서 |
|---|---|
| 「앞으로 ~하게 해」 「문체는 이렇게」 「배포는 이 순서로」 | `CLAUDE.md` 지침 로그 |
| 「이 뉴스가 왜 빠졌어」 「소스 추가해」 「이 기업 표기 통일해」 「한컴 관점 기준 바꿔」 | `프롬프트.MD` |
| 「작성 시각 바꾸자」 「오전엔 가져오기 안 해도 돼」 | `동료 가이드.md` (+ 시각 규칙은 `프롬프트.MD`) |
| 「관리자 화면에서 이건 어디서 해」 | `ADMIN-GUIDE.md` |
| 「이거 고쳐야 해」 「나중에 이것도 하자」 | `TODO.md` |
| 「이건 다 됐어」 | `TODO.md`에서 지우고 `ROADMAP.md`로 |
| 「상사에게 구조 설명해야 해」 「비용 어디로 나가」 「요약이 매번 달라지나」 | `ARCHITECTURE.md` |
| 「키 만료됐어」 「모델 바꿔」 「LiteLLM으로 옮겨」 | `LLM-PROVIDER-GUIDE.md` (작업 항목은 `TODO.md` 1순위 ①) |
| 「새 동료가 붙는다」 | `ONBOARDING.md` |

- 데이터(보고서 내용) 수정은 문서가 아니라 `/admin` draft → 검수 → 배포로 처리한다
- 코드 변경은 문서 갱신과 **같은 브랜치·같은 PR**에 묶는다(문서만 나중에 고치면 어긋난다)

## ⭐ 최우선 규칙
- **프론트엔드 개발 시 반드시 `design-preview.html`을 참고할 것.** 이 파일이 확정된 디자인 기준이다.
  - 색/폰트/레이아웃/컴포넌트 스타일은 모두 이 파일을 따른다.
  - `AX-Biz-Radar-기획.md`의 디자인 서술(레이더/오렌지/다크 컨셉)은 **폐기**되었고,
    디자인은 `design-preview.html`(Template 기반 라임 톤)이 우선한다.
- **관리자 페이지의 존재를 대시보드(메인)에 절대 노출하지 말 것.**
  - nav·푸터·어디에도 `/admin` 링크/버튼/힌트를 두지 않는다.
  - 관리자 페이지는 URL을 직접 아는 사람만 접근한다.

## 확정된 디자인 (design-preview.html 기준)
- **톤**: Template(`Template/index.html`)의 비주얼 언어를 그대로 차용한 밝은 에디토리얼 스타일.
- **색**: 라임 `#c8f200`, 베이지 `#f7f5f0`, ink `#111`, 라벨용 `lime-600 #7ba500`.
- **폰트**: 제목 `Space Grotesk`(font-display), 본문 `Inter`. 대형 헤딩은 `tracking-tight/tighter`.
- **컴포넌트**: 라임 nav(`bg-lime/90` blur), 흰 라운드 카드(`rounded-[24px]` + `shadow-xl shadow-ink/5` + hover lift),
  `text-lime-600` 대문자 미니 라벨, `bg-ink` 다크 대비 카드, `.sn-reveal` 등장 애니메이션.
- **레이아웃 (한 화면)**:
  - 상단: 라임 nav (브랜드 + 태그라인 좌측 / Report Date 우측). **관리자 버튼 없음.**
  - 좌측(~42%): **지식 그래프** 다크 카드 — 임의 노드-링크 관계망(기업↔태그, 중앙 AX 허브) +
    하단 통계(취합 데이터 총 개수 / 추적 기업 / 분석일 / 태그 수). 크기 현행 유지.
  - 우측(~58%): **세로 스윔레인** — 대기업 → 중견기업 → 스타트업·중소 (순서 고정).
    각 섹션은 전체 폭 사용, 카드 `lg:grid-cols-2`로 2장씩 넓게 배치. 우측 영역 세로 스크롤.
  - 하단: 흰 라운드 카드 월 캘린더 (데이터 날짜 라임 점, 선택 시 라임 채움, 기본=최신 날짜).
  - 기업 카드: 기업명 + 요약 → 클릭 펼침 → 주요 내용 / 시사점 / **한컴 인사이트(라임 박스)** /
    출처·상세모니터링(Confluence) 링크 / 태그 칩.
- **태그라인**: "AX 레이더 : 시장의 동향 포착에서 인사이트까지"

## 확정된 기술 스택
- 프론트: 바닐라 HTML/CSS/JS + Tailwind CDN (빌드 단계 없음).
- DB: **Cloudflare D1** (서버리스 SQLite), Pages Functions에 `DB` 바인딩.
- 백엔드/REST: Cloudflare Pages Functions (`/api/*`).
- 배포: Cloudflare Pages + GitHub 레포 자동 배포.
- 관리자 인증: 단순 숫자 PIN. 서버(Function)에서 `ADMIN_PIN` 환경변수와 대조하여 검증.

## ⭐ 배포 워크플로 (2026-06-12 사용자 확정)
- **코드(UI/기능) 변경**: 항상 **stg 브랜치에 먼저 배포** → 사용자에게 스테이징 URL 공유 → **확인 후** main 배포.
  - 스테이징: `npx wrangler pages deploy public --project-name=ax-biz-radar --branch=stg --commit-dirty=true --commit-message="ascii"`
    → `https://stg.ax-biz-radar.pages.dev` (바인딩·시크릿 모두 동작 확인됨)
  - 본 반영: 같은 명령에 `--branch=main`.
  - 예외: 사용자가 명시적으로 "바로 배포"라고 한 경우만 main 직행.
- **데이터(보고서) 변경**: 코드와 무관 — `/admin` 가져오기/수동입력 → draft → 검수 → 배포 버튼 (사이트 `/preview`에서 draft 합본 확인).
- 두 트랙은 독립: 코드 스테이징=stg 브랜치 URL, 데이터 미리보기=`/preview`(draft 합본).

## ⭐ 통합 검수 창구 (2026-06-12 사용자 확정)
- **stg 도메인을 단일 "배포 전 검수" 창구로 사용.** `https://stg.ax-biz-radar.pages.dev/preview` = **새 코드(stg 브랜치) + draft 데이터(공유 D1)**를 한 화면에서 확인.
  - `/admin`의 "검수 미리보기 ↗" 버튼이 이 URL로 연결됨(크로스오리진이라 PIN 1회 추가 입력).
- **그래서 main에 코드 배포할 때는 stg 브랜치도 항상 함께(또는 먼저) 배포해 동기화**할 것 — 안 그러면 검수 화면이 옛 코드로 draft를 보여줘 오해 소지.
  - 데이터만 바뀌는 경우엔 stg 브랜치가 이미 main과 같으니 추가 배포 불필요.
- prod(`ax-biz-radar.pages.dev`)=발행 코드+발행 데이터(공개), stg 도메인=스테이징(검수).

## ⭐ 협업 배포 프로세스 (2026-06-12 사용자 확정) — 2인(관리자 + 협업자)
- **협업자(또는 그 Claude Code)는 절대 `main`에 직접 푸시/배포하지 않는다.** 항상 **새 브랜치 → PR**까지만.
  - 코드 변경 요청 시: 새 브랜치 생성 → 커밋 → 푸시 → **PR 생성**. (`gh` 없으면 git push 후 PR은 사용자/웹에서)
  - `main` 직접 푸시·`wrangler pages deploy --branch=main`(운영 직접배포) **금지**.
- **운영 배포 = `main` 머지 시 GitHub Actions(`deploy.yml`)만.** 운영 토큰은 GitHub Secrets에만(노트북엔 두지 않음). "머지 = 배포".
- **PR/브랜치 푸시 시 자동**: `preview.yml`이 **단일 스테이징**(`stg.ax-biz-radar.pages.dev`)으로 배포(PR마다 새 주소 X) + PR 댓글, `pr-check.yml`이 JS 문법 검사.
- 신규 협업자 셋업은 `ONBOARDING.md` 참고. 데이터 변경은 코드 아님(`/admin` draft→배포).
- 관리자가 GitHub에서 1회 설정: **`main` 브랜치 보호(PR+승인 필수, 직접 푸시 차단)**, 협업자 collaborator 초대, 운영 토큰은 협업자에게 미공유.

## ⭐ 데이터 문체 기준 (2026-08-10 사용자 확정)
> 보고서 항목(`keyPoints` 주요내용 · `implications` 시사점 · `hancomInsight` 한컴 인사이트)에 적용.
> 대외 배포물이므로 **공문 수준의 문어체**를 쓴다. "AI가 쓴 티"가 나면 안 된다.

- **개조식 + 명사형 종결**: 서술형(`~이다 / ~한다 / ~했다`) 금지.
  **체언 종결을 기본으로 한다** — `~예정 / ~선점 / ~미공개 / ~추적할 대상 / ~필요`.
  `-ㅁ/-음`(`~함 / ~됨 / ~임 / ~음`)은 체언으로 끝내면 뜻이 뭉개지는 경우, 즉 부정·수동을 살려야 할 때만 쓴다
  (`확인되지 않음`, `범위가 ~에 한정됨`). `~임`을 습관적으로 붙이면 문장이 늘어진다. (2026-08-12 확정)
- **시사점·한컴 인사이트는 불릿 여러 개로 쓴다**: 논지 1개 = 불릿 1개. 장문 한 덩어리로 쓰지 않는다.
  배열 원소가 곧 화면의 불릿 1개이며, 항목이 하나여도 불릿으로 렌더된다.
- **em dash(`—`) 금지**: 쉼표·콜론(`:`)·`~로` 연결로 대체한다. 기존 항목의 `라벨 — 내용` 형식도 신규 작성 시 쓰지 않는다.
- **온점(`.`) 금지**: 문장을 쉼표로 이어 한 덩어리로 만든다. 예외는 버전 표기(`Apache 2.0`)뿐.
- **구어·가벼운 표현 금지.** 실제 지적 사례:

| 쓰지 말 것 | 쓸 것 |
|---|---|
| 열린 것은 ~뿐 | 공개된 범위는 ~에 한정됨 |
| 옮기기 어려우며 | 이관에 제약이 있으며 |
| ~수준에 머묾 | ~형태로 제한됨 |
| 공짜로 쓸 수 있는 | 무상 이용이 가능한 |
| 가져다 쓰는 것도 합법 | 설계 차용도 법적으로 허용됨 |
| 물음이 나올 여지가 커짐 | 질의가 제기될 가능성이 높아짐 |
| 지켜볼 것은 | 모니터링 대상은 |
| 여기 들어오지 못함 | 구조상 진입이 불가함 |
| 다듬어 내놓은 것 | 보완하여 외부에 공개한 것 |
| 바로 쓰는 / 물려받은 | 즉시 사용하는 / 사전 반영된 |
| 받는 사람 권한을 다시 확인 | 수신자 권한을 재검증 |
| 얹는 방식 / 잇는다는 | 추가하는 방식 / 연결한다는 |
| ~축 (대응 축, 경쟁 축, 차별화 축) | 경쟁 지점 / 비교 기준 / 차별화 요소 (구체적으로) |

- 영어 직역 명사구 금지는 전역 지침과 동일(통증→불편한 점, 무마찰→번거로움 없이 등). 기술 용어(capability, workerd, MCP, Gatekeeper 등)는 원문 그대로 둔다.
- **길이는 기존 항목 수준을 넘지 않는다**:
  주요내용 **3~8개, 각 50~150자** / 시사점 **2~5개, 각 60~140자** / 한컴 인사이트 **2~5개, 각 60~140자** /
  태그 **5~10개**(발행 시 10개까지만 저장됨) / 본문 총 **1,200~1,900자**.
  시사점·한컴 인사이트의 「1개, 200~570자」 기준은 줄글이 길어져 읽기 어려웠던 문제로 **폐기**했다. (2026-08-12 확정)
  상세 배경은 레이더에 넣지 않고 **컨플루언스에 별도 작성**한다.
- 배포 시 `_publish.js`가 요소당 2,000자·배열 50개로 자르므로 한도 초과분은 소실된다. 작성 후 길이를 확인할 것.

---

## 데이터 / API (계획 기준 — AX-Biz-Radar-기획.md 참조)
- 스키마: `reports(date PK, companies TEXT(JSON), updated_at)`. 날짜 PK upsert(덮어쓰기).
- 엔드포인트: `GET /api/dates`, `GET /api/reports?date=`, `POST /api/reports`(PIN 검증, 자동화 진입점).

---

## 지침 로그 (시간순 누적)
- 디자인은 `design-preview.html` 기준. (사용자 확정)
- 코드 변경은 stg 브랜치 선배포→확인 후 main 반영. 데이터는 /admin draft→검수→배포. (2026-06-12 사용자 지시)
- 검수 환경 주소를 `preview.ax-biz-radar.pages.dev` → `stg.ax-biz-radar.pages.dev`로 변경(데이터 미리보기 `/preview` 페이지와 이름 충돌 해소). 배포 브랜치도 `preview`→`stg`. (2026-06-19 사용자 지시)
- 기획안의 레이더/오렌지/다크 컨셉 → Template 라임 톤으로 전환. (사용자 지시)
- 칸반: 가로 3컬럼 → 세로 스윔레인 + 카드 2장 grid. (사용자 지시)
- 관리자 입력 버튼 삭제, 관리자 페이지 존재 비노출. (사용자 지시)
- 대시보드 기간 기본값을 **월간 → 주간**으로 변경(일/주/월 토글은 그대로 유지). 이전 월간 기본값은 상사 피드백 근거였으나 이번 지시로 대체. (2026-08-10 사용자 지시)
- 보고서 항목 문체를 **개조식·명사형 종결 + 공문 문어체**로 확정. em dash·온점·구어 표현 금지, 길이는 기존 항목 수준 유지, 상세는 컨플루언스로 분리. → 위 「데이터 문체 기준」 절 (2026-08-10 사용자 지시)
- 시사점·한컴 인사이트를 **불릿 여러 개(2~5개, 각 60~140자)**로 전환. 기존 「1개 장문」 기준 폐기. 종결은 **체언 종결 기본**이고 `~임` 남용 금지. 항목이 1개여도 불릿으로 렌더. 발행 단계에 명사형 끝 온점 자동 제거 추가(`functions/_style.js`). 기존 데이터 460건은 순차 재작성. (2026-08-12 사용자 지시)
- 한컴 Agentic OS 를 **AI 오케스트레이션 AX 플랫폼**으로 규정. 공식 정의는 「기업의 모든 시스템과 데이터를 연결하고, 여러 AI 에이전트의 협업으로 실제 업무 수행을 지원하는 기업용 AI 운영 체제」이며, 문서·결재는 여러 업무 중 하나일 뿐 플랫폼의 정체가 아니다. 한컴 인사이트의 비교 기준은 에이전트 협업 조율 / 권한·거버넌스 / 모델 중립 / 배포·주권 / 레거시 연동을 우선한다. 제품 구성은 `한컴 에이전틱 OS` + `한컴 오케스트레이션 그리드`(노코드 워크플로 자동화) + `한컴 데이터 로더`(문서→데이터, ODL). 배포는 클라우드·온프레미스·폐쇄망 모두 지원. 근거는 공식 사이트 [ai.hancom.com](https://ai.hancom.com/ko). 상세는 `프롬프트.MD` 「한컴 Agentic OS 는 무엇인가」 절. (2026-08-13 사용자 지시, 공식 자료로 검증)
- 「축」(대응 축·경쟁 축·차별화 축) 표현 금지. AI가 쓴 티가 나므로 경쟁 지점·비교 기준·차별화 요소로 구체화한다. (2026-08-13 사용자 지시)
- 위클리 픽 목록(`/weekly`)을 **회차 썸네일 그리드**로 전환. 인스타 피드처럼 회차가 쌓여 보이는 것이 목적이고 참고는 `instagram.com/ai_freaks.kr`. 결정 3가지:
  ① 커버는 **사진을 쓰지 않고 활자·색만으로 자동 생성**한다(이미지 저장소가 없고, 매주 사진을 구하는 손과 저작권 판단이 늘어난다). 배색 3종을 회차 번호로 순환해 구분감을 만든다.
  ② 카피는 **고정 양식**(「8월 4주차 / AX Biz Radar News」 + 동향·주목 건수 + 키워드 3개)이라 매주 사람이 쓸 것이 없다. 참고 계정의 밈 톤은 쓰지 않는다(공문 문어체 기준과 충돌).
  ③ `/weekly`는 목록, 상세는 `/weekly?w=`. 공유 텍스트에는 늘 `?w=`가 붙으므로 단톡방 유입 동선은 그대로다. 카톡 OG 이미지는 실제 PNG가 필요(래스터화)해 Phase 2로 둔다. (2026-08-24 사용자 지시)
- **기사 사진 자동 수집 폐기, 관리자 직접 업로드로 전환**. 확인한 것 — ① `og:image` 추출과 핫링크는 기술적으로 되지만(3개 매체 15건 전부 성공) 우리 CSP `img-src 'self' data:` 가 외부 도메인 이미지를 막는다. ② CSP 를 열어도 원본이 사라지면 지난 회차가 소급해서 깨진다. ③ 해상도가 15건 중 1건만 620px 판을 채웠다(zdnet 548~640 / etnews 500~700 / aitimes 1,000~1,280). 그래서 **관리자가 올린 파일만 싣는다**(R2 `ax-biz-radar-img`). **선택 입력**이며 안 올리면 활자·도형 판으로 나가 매주 의무가 되지 않는다. **출처·권리 근거는 필수**이고 비면 서버가 버린다. 기사 사진을 내려받아 올리는 것은 링크가 아니라 복제라 더 위험하다. (2026-08-24 사용자 지시, 실측 근거)
- **뉴스레터를 상세 페이지와 별개 페이지(`/news`)로 둔다.** 같은 회차 데이터를 다른 조판으로 읽는 것이고 상세(F안 매거진)는 그대로 유지한다. 상세 상단의 표지 썸네일을 누르면 **페이지를 옮기지 않고 겹쳐 띄운다**(읽던 자리를 잃지 않게). 조판은 `news.js` 한 곳에서만 정하고 상세는 `window.AXNews` 로 불러 쓴다. 넘기기는 스와이프·클릭·← →·space·PageUp/Down·Home/End. (2026-08-24 사용자 지시)
- **슬라이드 비주얼 판에는 큰 제목을 넣지 않는다.** 바로 아래 헤드라인과 같은 문장이라 두 번 읽힌다. 사진이 없는 판은 **활자 없이 관계망 도형만**(모티프는 대시보드 지식 그래프). 기업명·분류·날짜·로고는 두 판 모두 얹는다. (2026-08-24 사용자 지시)
- **공유 텍스트 복사 폐기, 웹훅 메시지 하나로 통일.** 문구 형식은 사용자 지정 — 레이더 이모지 + 주차 제목, 인사 두 줄, 📌 수치, 픽 건수·신규 진입 기업, 글머리기호 목록, 상세 페이지 링크. **아웃트로는 붙이지 않는다**(자세한 것은 상세 페이지에서 읽는다). 발송은 발행과 분리한 별도 버튼이고, 나갈 문구를 먼저 보여주고 확인받으며 이미 보낸 회차는 시각을 알려 다시 묻는다. (2026-08-24 사용자 지시)
- ~~**픽 사진은 상세 페이지에서 원본 비율·크기 그대로 싣는다.**~~ (같은 날 원상복구, 아래 항목 참조) 12:5 로 판을 고정해 두었더니 원본 비율이 1.33~2.55 로 제각각이라 위아래가 잘렸다. → 폐기. **이미지 출처를 화면에 표기하지 않는 것은 그대로 유지** — 관리자 입력에는 필수라 기록은 회차 데이터에 남는다. (2026-08-24 사용자 지시)
- **뉴스레터(`/news`)는 사이드바에 노출하지 않는다.** 페이지와 URL 은 그대로 두고 진입점만 뺀다 — 들어오는 길은 상세 페이지 상단의 표지 썸네일이다. 뉴스레터 마무리 장에서 목록으로 가는 링크는 **「Weekly Pick Board」**로 부르고, 「해당 회차 상세보기」는 두지 않는다(상단 「상세로 →」와 겹친다). (2026-08-24 사용자 지시)
- **픽 사진은 12:5 판에 맞춰 싣는다(원본 크기 지시 원상복구).** 원본 비율·크기 그대로 두었더니 사진마다 크기와 세로 길이가 제각각이라 픽이 이어지는 조판이 흐트러졌다. 판을 고정하면 순번·기업명·본문 3열이 매 픽에서 같은 자리에 온다. 잘리는 기준(위·가운데·아래)은 상세와 뉴스레터 슬라이드 **양쪽에서** 쓰고, 판보다 작은 사진은 레터박스로 메운다(밝은 판 베이지 / 다크 판 흰색 5%). 출처 미표기는 유지. (2026-08-24 사용자 지시)
- 위클리 픽 목록에 **「발행 예정」 자리**를 둔다. 발행 회차가 한둘일 때 그리드가 비어 보이는 것을 다음 회차 자리로 메운다. 어느 주가 예정인지는 **서버가 정한다**(`/api/weekly?list=1` 의 `upcoming`) — 기준은 「최신 회차 + 1주」가 아니라 **오늘이 속한 주**이고(한 주를 건너뛰면 최신+1 은 지난 주를 예정으로 내걸게 된다), 그 주가 이미 발행됐으면 자리를 내리지 않는다. 조판으로 「아직 아닌 것」을 말한다: 배색 3종을 쓰지 않고 **점선 테두리 + 베이지 바탕**, 링크가 아니라 `div`, 회차 번호는 붙이지 않는다(발행 시점에 정해진다). 표기는 `Coming soon` 라벨 + 주차 + `8/24 ~ 8/30 · 발행 예정`. 발행 회차가 0건이면 예정 자리가 아니라 기존 안내 문구를 쓴다. (2026-08-24 사용자 지시)
  - 활자만 두었더니 발행 커버의 큰 회차 번호가 채우던 우하단이 비어 단촐하게 보였다. 색을 주지 않는다는 원칙은 지키고 **텍스처·도형·점 하나**로 채운다: ① 대각 해칭(ink 5%), ② 점선 관계망 도형을 회차 번호와 같은 자리·같은 무게로(모티프는 대시보드 지식 그래프), ③ 상단 라벨 아래 라임 점 + 「이번 주 동향 수집 중」 한 줄. 예정 주차는 서버가 오늘이 속한 주로 정하므로 이 문구는 사실과 어긋나지 않는다. 라임 점이 이 카드의 유일한 색이며 판 바탕은 그대로 베이지·점선이다. (2026-08-24 사용자 지시)
- 위클리 픽 메시지의 **주요 동향 픽 제목을 자르지 않는다**. 32자 + `…` 로 줄였더니 제목이 끊겨 읽혔다. 두 줄로 넘어가도 전문을 싣는다. 링크 라벨은 굵게 한다. (2026-08-24 사용자 지시)
- **메시지 발송을 2단계로 나눈다** — [1차 리허설 발송]=테스트 방(`WEEKLY_WEBHOOK_TEST_URL`), [2차 전사 발송]=전사 라운지(`WEEKLY_WEBHOOK_URL`). 회수가 안 되는 발송이라 눈으로 확인하는 단계를 강제한다. 확인한 것 — ① stg 에서 시험하는 것으로는 부족하다: stg 는 Preview 환경이라 `SITE_ORIGIN` 이 달라 링크 주소가 실제와 다르고, 「본 것」과 「나갈 것」이 애초에 같은 문자열이 아니다. 같은 환경에서 1차·2차를 돌리면 문구가 글자 단위로 같아지고, 그 「같음」을 SHA-256 앞 16자로 확인한다(1차 뒤 데이터를 고치면 2차가 막힌다). ② **관리자 화면에 Chat 렌더 미리보기를 만들지 않는다** — 렌더러를 흉내내는 것이라 「본 것과 보낸 것」이 갈라지고, 정작 걱정하는 아바타는 우리가 값을 알 수 없어 그릴 수 없다. ③ **웹훅 아바타·이름은 이 방식으로도 검증되지 않는다** — 방마다 웹훅 등록 시 각각 설정하는 값이고 우리 payload 에 없다(본문은 `text` 하나). `ax-biz-radar-icon.png` 는 코드가 참조하지 않는다. 두 방의 설정 일치는 Chat 웹훅 관리 화면에서 대조할 일이고, 매주 확인할 사안이 아니다. 발송 기록은 방별(`payload.notifyLog`)로 적는다 — 회차당 한 칸이면 1차가 「이미 보냄」으로 남아 2차에서 틀린 경고가 뜬다. (2026-08-24 사용자 지시)
- **라이브(발행된) 항목을 지우는 경로를 만든다.** 배포(`_publish.js mergeAndPublishDate`)는 병합이라 draft 에 없는 라이브 항목은 손대지 않는데, 그 사실이 화면에 드러나지 않아 「수동 입력에서 기업을 빼고 재배포 = 삭제」로 오해됐다(2026-08-25·08-24 각 5건이 남아 손으로 정리). 지우는 단위는 **기업이 아니라 「기업 1곳의 동향 1건」**이고, 삭제 신원은 배열 인덱스 + `(name, entryKey)` 대조로 정한다(키만으로 지우면 과거 중복 데이터에서 여러 건이 함께 사라지고, 인덱스만 쓰면 그 사이 데이터가 바뀌었을 때 엉뚱한 건이 지워진다 → 어긋나면 409). 지울 때 파생 데이터를 같이 맞춘다: `company_entries` 재구축 + Vectorize 재색인(줄어든 만큼 고아 벡터 정리). 그 날짜 항목이 0이 되면 `reports` 행 자체를 지운다(`/api/dates` 가 그대로 돌려주므로 빈 행을 남기면 캘린더에 데이터 없는 날짜가 점으로 남는다). 이미 발행한 위클리 픽은 스냅샷이라 바뀌지 않으며 이는 의도된 차이다. (2026-08-27 사용자 지시)
- **관리자 화면 명칭에서 「기업」을 「동향 항목」으로 바꾼다.** 「기업 삭제」가 그 기업의 전체 데이터를 지우는 것으로 읽혔다 — 실제로는 동향 1건이고, 게다가 그 버튼은 입력 목록에서 빼는 것일 뿐 아무것도 지우지 않는다. 「기업 삭제」→**「이 항목 빼기」**, 「기업 목록/기업 추가」→**「동향 항목/+ 항목 추가」**, 검수 카드의 「삭제」→**「초안 삭제」**(라이브 삭제와 구분). 무엇을 지우는 버튼인지는 라벨만으로 구분되게 하고, 되돌릴 수 없는 것은 확인 창에 기업명·날짜와 「남는 것」을 먼저 보여준다. (2026-08-27 사용자 지시)
- **정부·부처·공공기관은 항목 주체로 쓰지 않고, 참여 기업으로 나눠 등재한다. `공공·기관` 4번째 분류는 만들지 않는다.** 과기부 독파모 학습 데이터 개방 건(2026-08-27)을 기관 이름으로 올리려다 정한 것이고, 기관을 기존 분류에 넣으면 「과기부 = 대기업」이 되어 화면 라벨이 사실과 어긋난다. 늘리지 않는 이유 — ① `프롬프트.MD` 「분류는 기업 규모 하나만 본다」 기준에 민간/공공이라는 다른 성격의 축을 한 칸 끼우면 스윔레인이 두 기준으로 섞여 나뉜다, ② 라이브 기업명 88개가 전부 기업이고 정부 사업 기사는 그동안 참여 기업으로 나눠 등재해 왔다(2026-08-26 사이버보안 특화 AI 국책과제 → `SK텔레콤`·`네이버클라우드`·`업스테이지` 3건). 상설 4번째 레인은 대부분의 날에 0건으로 비어 보인다, ③ 분류 세 값이 코드 8곳에 복제되어 있고 `functions/api/reports.js` 의 `CATEGORIES` 검증을 빠뜨리면 **항목이 조용히 버려지며**(`dashboard.js` 도 목록 밖 분류를 화면에서 누락) `public/index.html` 스윔레인은 하드코딩이라 `design-preview.html` 까지 함께 고쳐야 한다. 나누는 방법은 기관의 발표문이 아니라 **각 기업이 무엇을 했는가**로 다시 쓰는 것이고, 기업별로 선택이 갈린 부분(전량 개방 4곳 / 의무량 50%만 개방한 `LG AI연구원`)이 곧 그 기업의 동향이다. 참여 기업이 없는 순수 정책·제도 발표는 항목을 만들지 않고 영향받는 기업 항목의 시사점에 녹인다. 상세는 `프롬프트.MD` 「정부·부처·공공기관은 항목 주체로 쓰지 않는다」 절. (2026-09-01 사용자 지시)
- **라이트·다크는 색을 하나씩 덮는 것이 아니라 토큰으로 갈아탄다.** 다크 모드에서 대시보드 본문(주요 내용·시사점·한컴 인사이트)이 어두운 바탕에 어두운 글자로 깔려 읽히지 않았고 다른 페이지도 같았다. 원인은 `dark.css` 가 유틸리티 클래스를 하나씩 덮는 방식이라 새로 쓰는 투명도가 매번 누락된 것이다(`text-ink/85`·`/90`·`/55`·`/35` 와 arbitrary 값 `text-ink/[.06]` 까지 실측 34종). 그래서 Tailwind config 의 색을 `rgb(var(--c-ink) / <alpha-value>)` 로 두고 `dark.css` 에서 변수만 뒤집는다 — 투명도가 몇이든, 앞으로 새로 쓰든 자동으로 모드를 따른다. 예외 두 가지: ① **라임 판 안에서는 ink 를 라이트 값으로 되돌린다**(라임은 두 모드에서 같은 밝은 색이라 그 위 글자는 늘 어두워야 한다). `header`·`nav` 는 다크에서 라임 바가 잉크로 바뀌므로 제외한다. ② **`bg-ink` 솔리드 판과 `bg-white` 카드는 토큰을 쓰지 않고 고정 어두운 색으로 박는다**(안쪽이 `text-white` 기준으로 짜여 있다). `/admin`·`/preview` 는 `dark.css` 를 읽지 않는 라이트 전용 화면이라 literal 색을 그대로 둔다. (2026-09-01 사용자 지시)
- **뉴스레터 픽 판(사진 판·도형 판)에는 한컴 로고를 얹지 않는다.** 판 안의 그림은 그 기업의 동향을 가리키는 것이라 그 위에 우리 로고가 오면 출처로 읽힐 여지가 있다. 「기업명·분류·날짜·로고를 두 판 모두 얹는다」(2026-08-24)에서 **로고만 제외**하고 기업명·분류·날짜는 그대로 둔다. 제호(로고 + `AX Biz Radar News`)는 **표지·마무리 장과 겹쳐 띄우기 썸네일**에만 남긴다 — 회차 발행물임을 말하는 자리라 그림 위가 아니다. 조판은 `news.js` 의 `panelHead` 한 곳이고, 이중 관리 중인 `weekly-preview-news.html` 도 같이 고친다. (2026-09-01 사용자 지시)
- 위클리 픽 **주요 동향 수량 제한 폐기**. 「최대 5건」을 없애고 그 주에 주목할 것이 많으면 많이 싣는다. 선택은 **기업별이 아니라 건별**이라 같은 기업의 동향이 그 주에 둘이면 두 줄로 뜨고 따로 고른다. 서버 `MAX_PICKS=50`은 폭주 방지용 상한이며 편집 기준이 아니다. (2026-08-24 사용자 지시)
- **수동 입력의 「이 항목 빼기」는 라이브 삭제가 아니라 편집 목록에서만 빼는 것으로 확정한다.** 2026-08-31 에 넣은 「불러와서 고친 수동 입력은 빼기 = 배포 시 라이브 삭제」 동작을 폐기한다 — 배포는 병합으로 되돌리고, 불러오기 기준선(`settings` 의 `manual_base:<date>`)·삭제 예정 안내(`willRemove`)·배포 전 dryRun 계산·`STALE_BASE` 409 를 모두 없앤다. 폐기 근거 — ① 되돌릴 수 없는 삭제가 「저장·배포」라는 일상 동작에 딸려 붙어, 확인창에 무엇이 지워지는지 적어도 사고가 났다(2026-09-02 에 사용자가 의도적으로 지운 항목이 검수 대기로 되살아나 배포 한 번이면 복구되는 상태가 된 사례), ② 라이브에서 지우는 경로는 이미 `[🗑 라이브 항목 삭제]`(`/api/dev/live`)로 따로 있고 그것으로 충분하다, ③ `ADMIN-GUIDE.md` 와 관리자 화면 안내문은 계속 「빼기는 삭제가 아니다」로 적혀 있어 코드와 문서가 어긋나 있었다. **라이브 삭제는 `/api/dev/live` 한 곳에서만 일어난다.** 「기업 1곳의 동향 1건」 단위 삭제와 파생 데이터 정리 규칙은 2026-08-27 지시 그대로 유지한다. (2026-09-02 사용자 확정)
