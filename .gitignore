# dependencies
node_modules/

# wrangler / cloudflare
.wrangler/
.dev.vars
.env
.env.*
!.env.example

# oh-my-claudecode 세션 상태 (앱과 무관)
.omc/

# misc
.DS_Store
*.log

# 루트 참조 이미지(설계 참고용, 서빙 안 함)
/*.png

# 이 프로젝트 산출물이 아닌 작업 자료(레포에 올리지 않음)
/GTM-TWIN/
/research/

# 키·모델·게이트웨이 교체 절차 — 로컬 참고용이며 커밋 대상이 아니다(CLAUDE.md 문서 지도).
# 추적 대상이 아니었을 뿐 무시 규칙이 없어 `git add .` 로 딸려 들어갈 위험이 있었다.
/LLM-PROVIDER-GUIDE.md
