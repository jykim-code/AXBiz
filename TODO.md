# AX Biz Radar — 할 일 (고도화·수정)

> **이 파일에는 앞으로 할 일만 둔다.** 완료 이력은 `ROADMAP.md`에 있다.
> 새 항목은 아래 「1순위」 또는 주제별 백로그에 추가하고, 끝나면 `ROADMAP.md` 완료 절로 옮긴다.
> 작업을 시작한 항목은 「지금 하는 것」으로 올린다.

---

## 🟡 지금 하는 것

- [ ] **Vectorize 재색인** — 기업 표기 통합(8/24)으로 바뀐 4건의 벡터가 옛 영문 이름으로 남아 있다.
  `_rag.js` 의 `companyToText` 가 `기업명 (분류)` 를 임베딩에 넣기 때문. `/api/ask` 검색에만 영향(323건 중 4건)
  - `POST /api/reindex {"dates":["2026-07-09","2026-08-18","2026-08-19"]}` (관리자 PIN 필요)

- [ ] **발행본 스냅샷 구멍** — `save` 액션이 `status` 를 보지 않고 `payload` 를 덮어써서 발행 후 [저장]만
  눌러도 공개된 링크 내용이 바뀐다. 기획 3절 원칙 2(스냅샷 고정)에 정면으로 어긋난다
  - 정석은 초안용 payload 를 별도 컬럼으로 두는 것. 관리자 편집 동선([저장]→[미리보기]→[발행])도 함께 봐야 한다
  - 표시 불일치(`stats.picks`)는 8/24 에 별도로 막았고, 원칙 위반 자체는 남아 있다

---

## 1순위 — 2026-08-13 확정 요구사항

> 순서 근거: ①이 모델 품질·자동화·비용의 선행 조건이고, ②는 코드 없이 문서 편집만으로 오늘 적용 가능하다.
> ③은 ① 결과에 따라 모델·비용·실행 횟수가 달라지고 ②의 시각·수집 창 결정을 물려받으므로 뒤에 둔다.

- [ ] **① LLM 게이트웨이 LiteLLM 전환** ⭐선행
  - 현재 AI크루 OpenRouter 키를 6곳이 공유(RAG 검색·기업 요약·기간 종합·태그 추천·컨플 추출·요약 일괄)
  - 회사 프로젝트 LiteLLM 키 발급 신청 필요
  - 전환 후 성능이 높은 모델 사용이 가능해져, 자동 취합을 클로드 앱 스케줄링에서 서버로 이관 가능
  - 선행 작업: 엔드포인트 URL이 6개 파일에 하드코딩 → `functions/_llm.js` 공용 헬퍼로 통합, 환경변수 이름을 게이트웨이 중립으로 변경
  - 확인 사항: LiteLLM 엔드포인트가 **공개 HTTPS**여야 Pages Functions에서 접근 가능(사내망이면 Cloudflare Tunnel), 추론형 모델은 `max_tokens`·reasoning 재조정, 화면 표기 모델명 갱신
  - 절차·검증 순서는 `LLM-PROVIDER-GUIDE.md` 시나리오 C. 구 키 폐기는 운영 검증 이후
- [ ] **② 탐색 프롬프트 잔여분** — `프롬프트.MD` 한 파일 편집으로 끝나는 묶음
  - 반영 완료(8/12~8/13): 문체·양식, 동향 유형 9종, 소스 4개 매체, 편입 트리거 A~J, 한컴 인사이트 도출 기준(공식 제품 범위)
  - ~~회차 작성 시각·수집 창 규칙 명시~~ → **2026-08-13 반영 완료**. `프롬프트.MD`에 「작성 시각과 수집 범위」 절 추가(10:00 작성 / 17:30 갱신, 「과거 24시간」 폐기, 수집 범위는 직전 작성 시각부터, 상단 `조사 범위` 줄에 분까지 기록). ③의 수집 창 설계가 이 결정을 물려받는다
  - **섹션 C 처리 방침** (2026-08-13 실물 확인, 결정만 남음)
    - 섹션 C = 「신규 Agentic OS·AX 플랫폼 레이더」 **표 형식 누적 레지스트리**
      (제품명·제공사·발표일·등급·포지셔닝·배포 형태·한컴 중첩 영역·상태). 변동 없는 항목은 재기재하지 않음
    - 8/12 회차 실측: **S·A급은 섹션 A에도 기재되어 레이더에 이미 적재**됨(네이버웍스=A-2, AI/Cockpit One=A-1).
      레이더에 없는 것은 **B급 2건**(Symphony by Wix, SUPERAGENT 3.0)이고 둘 다 한컴 중첩 영역이 「비중첩」
    - 섹션 C에는 기업이 아닌 **시장·정책 동향**도 포함(정부 사이버보안 사업, 모두의 AI 공모, 독파모 평가)
    - 정할 것 ① B급을 레이더에 넣을지 — 넣으려면 표 형식을 마커 형식으로 바꿔야 하고, S급이 A·C에 동시
      등장해 같은 사안이 두 항목으로 적재될 위험을 막아야 함 (**권고: 넣지 않고 현행 유지**)
    - 정할 것 ② **S·A급은 섹션 A에도 항목으로 쓴다**를 지침에 명문화 (현재 관행일 뿐 규칙이 아니라 누락 구멍)
    - 정할 것 ③ **등급·기준 체계 통일** ⭐선행 — 컨플은 `S·A·B 등급` + `등재 기준 a~e`를 쓰고 `프롬프트.MD`는
      `편입 트리거 A~J`를 쓴다. 두 체계가 병존하고 대응표가 없으며 a~e 정의는 어디에도 적혀 있지 않다.
      **회차 작성자에게 a~e 정의를 확인**한 뒤 트리거 A~J와 대응표를 만들어야 함
    - 정할 것 ④ B급이 상위 등급으로 올라가면 그 회차에 섹션 A로 승격 + `#신규편입` 태그 (없으면 영구 미적재)
    - 정할 것 ⑤ 시장·정책 동향의 자리 — 레이더 스키마는 기업 단위라 담을 곳이 없다. 컨플 전용으로 둘지,
      기업 항목의 시사점·태그로 녹일지
- [ ] **③ 데이터 취합 자동화 스케줄 설계** — ① 이후 착수, ②에서 정한 시각·수집 창을 물려받는다
  - 레포에 cron 설정이 없어 실행 주체부터 결정해야 한다(Pages Functions는 cron 미지원)
    - GitHub Actions cron으로 관리자 엔드포인트 호출: 가볍고 로그 확인이 쉬움, 크론이 UTC라 한국 시각 환산 필요
    - 별도 Worker에 Cron Triggers + D1·Vectorize 바인딩 공유: 플랫폼 내에서 완결, 배포 대상 증가
  - **컨플 경로를 어떻게 둘지 결정** (이 항목의 갈림길, ②와의 관계가 여기서 갈린다)
    - 전제(2026-08-13 사용자 확인): 회차 페이지의 실질 용도는 **① 컨플 누적 기록 ② 카드의 「상세」 링크 대상**이다.
      상위 보고 자체가 목적은 아니며, 없어도 되지만 있으면 낫다는 판단
    - 확인된 의존: `confluenceUrl`이 대시보드 카드·탐색 결과·질의응답 패널 **세 곳의 「상세」 링크**로 렌더된다.
      값은 가져온 페이지 URL이므로 컨플이 없어지면 세 곳이 모두 빈다
    - **경로 C (권장): 방향을 뒤집는다.** 자동화 → draft → 검수 → **배포 시점에 컨플 페이지를 결과 아카이브로
      생성·갱신**. 누적 기록과 「상세」 링크가 유지되고, 작성·재수입 왕복과 LLM 이중 호출이 없다.
      필요 조건은 Confluence **쓰기 권한**(현재는 읽기만 사용)과 아카이브 페이지 템플릿·소유자 정리
    - 경로 A: 자동화가 컨플 페이지를 작성하고 기존 가져오기로 다시 읽는다. 화면·운영 변경이 가장 적지만
      같은 내용을 쓰고 다시 읽는 왕복이라 LLM 호출과 실패 지점이 두 배
    - 경로 B: 컨플을 건너뛰고 draft만 만든다. 구현은 가장 단순하나 「상세」 링크를 레이더 내부 상세로
      돌리거나 제거해야 하고, 누적 기록이 없어진다
    - 경로 C·A를 택하면 `confluenceUrl`이 아카이브 생성 후에야 확정되므로, **URL 되기록 단계**가 필요하다.
      링크 필드만 갱신하고 재색인·요약 재생성은 건드리지 않는 경로로 만든다
    - 어느 경로든 함께 정할 것: 섹션 B·종합 시사점·Forward Watch처럼 **레이더에 적재되지 않는 사람용 서술**을
      계속 만들지 여부. 아카이브 방식으로 가면 이 서술은 사라진다
  - 실행 시각·횟수 결정 — ②에서 확정한 10:00·17:30을 승계하거나 자동화라면 횟수를 늘릴지 판단
  - **수집 창 구현**: ②에서 「직전 작성 시각부터」로 정한 규칙을 실제 상태값으로 만든다(직전 실행 시각을 기록하고 그 시점부터 수집). 문서 규칙만으로는 기계가 지킬 수 없는 부분
  - 주말·공휴일 처리, 실패 재시도와 알림 경로
  - 실행 1회당 토큰 사용량 측정 후 횟수 결정
  - 사람 개입 지점 고정: 자동 취합은 draft까지, 검수·배포와 시사점·한컴 인사이트 보강은 사람
- [ ] **④ Agentic OS wiki 구축 및 인사이트 도출 연동** — 상세는 아래 「데이터 커버리지 확장」
  - 판정 기준을 프롬프트에 고정하는 방식에서 지속 갱신되는 지식 저장소 참조 방식으로 전환
  - 참조 경로 3안 중 선택: 사람이 회차 작성 시 참조 / 자동 취합 프롬프트에 컨텍스트 주입 / RAG 색인 후 검색 대상 포함(레이더 데이터와 구분 필요)
- [ ] **⑤ 검색 후속 질의 + 답변 품질**
  - 현재 `/api/ask`는 `question` 하나만 받는 단발 구조로 대화 맥락이 없다
  - 후속 질의는 대화 이력 전달 방식(클라이언트 전달 vs 서버 세션 저장) 결정이 선행
  - 후속 질문을 독립 질의로 재작성하는 단계가 검색 품질에 직결
  - 답변 품질은 ①의 모델 교체와 연동, 인용 번호 유지·컨텍스트 예산·분당 10회 제한 재검토
- [ ] **⑥ 해외 기업 재무 정보** — 상장·비상장 2트랙, 「국내와 동일 제공」은 구조상 불가. 상세는 아래 「데이터 커버리지 확장」

### 함께 정리할 잔여 항목

- [ ] 요약·기간 종합 실패가 화면에서 조용히 사라지는 구조 → 관리자 화면에 실패 노출
- [ ] 기간 종합 캐시 프리워밍으로 첫 조회 지연 해소
- [ ] 라이브 데이터 문체 재작성 잔여분 관리 (460건 중 진행분)
- [ ] 표시명·분류 과거 축적분 정리 (`SK그룹` `현대차그룹` `롯데` `LG` `폴라리스그룹`)

---

## 주제별 백로그

### ⭐ 상사 피드백 — 남은 항목  _(2026-06-12 접수)_

> **핵심 전략은 이탈 방지**: 「이 업체 요즘 뭐하지 → … → 찾아볼까」 흐름의 마지막에서 사이트 밖으로
> 나가지 않도록 한 곳에서 궁금증이 완결되게 한다. 체류시간·이탈률을 핵심 지표로 본다.

- [ ] **B. 지식그래프 관계성 + 사용성** ⭐다음 유력 — 태그뿐 아니라 **엔티티 간 관계**(예: 올거나이즈 ↔ 조달청)를
      드러낸다(기업↔기관, 기업↔기업 관계 타입). 함께: 풀스크린·force 레이아웃·줌·드래그·노드 수 조절·축소 시
      라벨 가독성(현 concentric 임베드의 불편 해소). 관계 추출은 아래 「AI·지식그래프 고도화」와 연계
- [ ] **C. 외부 데이터 연결** — 인앱 검색이 내부 RAG에 더해 외부(웹·실시간) 소스까지 결합해 답하는 동선.
      인앱 인라인 답변 자체는 완료, 후속 질의·답변 품질은 1순위 ⑤
- [ ] **D. 서비스 지표(애널리틱스)** — 방문·체류·이탈은 **Cloudflare Web Analytics 도입으로 수집 시작(8/31)**.
      공개 페이지 6곳에 비콘, 수치는 Cloudflare 대시보드에서 본다. 남은 것 두 가지:
      ① 관리자 화면에서 지표를 보는 동선(현재는 CF 콘솔에 들어가야 한다)
      ② 사이트 안 행동 계측(카드 펼침·검색·질의·「상세」 클릭) — 비콘 기본 수집에는 없다
- [ ] 검수 직접 편집 강화 / 새 draft 알림 (컨플 정기 적재는 1순위 ③)

### 🌐 데이터 커버리지 확장

- [ ] **(자료 정리 필요) 한컴 Agentic OS wiki 구축** _(1순위 ④)_ — 사내 Agentic OS PDF를 정리해 제품·기능·아키텍처를
      한곳에 모은다. 이유: 공식 사이트([ai.hancom.com](https://ai.hancom.com/ko))는 마케팅 문구 수준이라 경쟁사
      판정 근거로 얕고, 그 결과 8/12 지침 검증 샘플에서 한컴 인사이트가 문서·결재 쪽으로 좁게 서술됨(2026-08-13 지적).
      담을 것: ① 제품 3종(`한컴 에이전틱 OS` / `오케스트레이션 그리드` / `데이터 로더`)의 범위와 경계
      ② 전문 에이전트 정식 라인업·명칭 확정(「도큐먼트 솔루션 에이전트」가 사내 표기인지 포함)
      ③ 아키텍처 구성 요소(사용자 포털·Agent Gateway·Agent Runtime·거버넌스·관측성)의 역할 구분
      ④ 배포 형태별(클라우드·온프레미스·폐쇄망) 지원 범위 ⑤ 공개 자료와 사내 자료가 어긋나는 지점.
      산출물은 `프롬프트.MD` 「한컴 Agentic OS 는 무엇인가」 절의 근거로 쓴다.
      PDF 수령이 선행 조건이며, 정리 수단으로 OMC `wiki_*` 도구 사용 가능
- [ ] **해외 경쟁사 재무 — 상장·비상장 2트랙** _(1순위 ⑥)_ — 국내는 DART 연동 완료, 해외는 미제공.
      상장사(`Microsoft` `Google` `NVIDIA` `IBM` `세일즈포스`)는 **SEC EDGAR**로 국내와 동일 수준 제공 가능.
      비상장사(`OpenAI` `Anthropic` `Mistral AI` `Cohere` `퍼플렉시티`)는 공시 의무가 없어 동일 제공이 불가하므로
      투자 유치·기업가치·인력 규모를 별도 항목으로 관리하는 방식이 필요.
      통화·회계 기준(분기 구분, USD) 차이로 기존 재무 화면의 비교 로직 조정도 함께
- [ ] **(기준 미정) AX 성숙도 단계** — 기업이 AX 시장 진입 이후 어느 단계까지 왔는지 나타내는 구분.
      분류(`category`)는 기업 규모 전용으로 확정했으므로(2026-08-12) 성숙도는 별개 기준이 필요하다.
      진입 여부만은 `#AX신규진입` 태그로 수집 중이고, 그다음 단계 구분이 미결.
      정할 것: ① 단계 정의(예: 발표·PoC·상용 레퍼런스·조달 수주·플랫폼화) ② 판정 근거 ③ 표현 방식(태그 vs 신규 필드).
      신규 필드로 가면 스키마·화면·컨플 가져오기까지 함께 손대야 하므로 태그 우선 검토
- [ ] **업계평균 대비 재무** — 동종 업종 평균과 해당 기업 비교. DART 대량 적재가 필요해 무거움
- [ ] **데이터 자동 수집** — 뉴스 RSS·검색 → LLM 초안 → 관리자 검토 후 등록 (실행 설계는 1순위 ③과 함께)
- [ ] **컨플 동기화 자동화** — 전체 페이지 일괄 동기화 버튼. 정기 실행은 1순위 ③에서 실행 주체·수집 창을 정한 뒤 붙인다

### 🤖 AI·지식그래프 고도화

- [ ] **지식그래프 관계 추출** — 본문에서 기업 간 관계(제휴·경쟁·리셀)를 LLM으로 추출 → `/admin` 검증(source·status)
- [ ] **(조사 필요) OKF(Open Knowledge Format) 접목** — Google Cloud OKF v0.1(2026-06): 지식은 마크다운 + YAML
      프론트매터 concept, cross-link은 그래프, `# Citations`. 접목 후보: ① 관계 그래프를 OKF concept(기업·기관·테마)와
      typed link로 모델링 ② D1 데이터를 OKF 번들로 내보내 Git·에이전트가 소비 ③ RAG를 큐레이션 concept로 보강.
      v0.1 신생 표준이라 저장소 교체는 하지 않고 표현·내보내기 레이어로만 차용. 1순위 ④의 wiki 형식과 함께 판단
- [ ] **위클리 픽 Phase 2** — 주차별 동적 OG(카톡 링크 미리보기), 월간판(`?m=`),
      초안 자동 생성 스케줄(1순위 ③에 얹는다), 조회 지표.
      Phase 1(`/weekly` 발행물 + 관리자 탭)은 `feat/weekly-insight` 브랜치에서 완료·스테이징 배포됨.
      기획서와 검증 결과는 `.omc/plans/weekly-insight-share-plan.md`
- [ ] **알림·구독** — 관심 기업·태그에 새 동향이 들어오면 이메일·슬랙 알림

### 🔎 사용성

- [ ] **기업 비교** — 2~3개 기업의 동향·재무를 나란히 보는 뷰
- [ ] **검색 필터·히스토리** — 기간·분류 필터, 인기 질문·최근 질문
- [ ] **대시보드 트렌드 차트** — 태그 빈도 추이, 분류별 활동량
- [ ] **소개(About) 페이지** — 서비스 소개·데이터 출처·갱신 주기 (사이드바 「소개」 항목)
- [ ] 모바일·접근성·성능 다듬기

**2026-09-01 사용성 점검에서 남은 것** _(헤드리스 Chrome 실측, 값은 그때 기준)_

- [ ] **nav 768px 겹침** — 태그라인(`index.html:70`, `hidden md:inline`)이 나타나며 브랜드 묶음이 267px → 543px 로 커져 **768px 에서만** 28px 넘친다. 태그라인이 「대시보드」와 겹쳐 읽히고 오른쪽 링크(기업·의견 보내기)가 잘린다. 가로 스크롤은 되지 않아 잘린 채로 보인다. 820px 이상·700px 이하는 정상 — 아이패드 미니·갤럭시탭 세로가 걸린다. 고치는 법: 태그라인 분기를 `md:` → `lg:` 로 미루기(한 글자) 또는 `flex-none` 풀고 태그라인에 `truncate`
- [ ] **첫 화면 2단계 분리** — `/api/reports/all`(584.9KB · 112일 · 동향 357건) 하나를 기다려 전부 그린다. 느린 4G 에서 첫 카드가 3.9초. `/api/dates`(1.4KB · 26ms) + 해당 기간만 먼저 그리고 그래프·통계를 뒤에 채우면 첫 화면이 0.3초대로 내려간다. 로딩 표시는 넣었으나(PR `747cf50`) **기다리는 시간 자체는 그대로**
- [ ] **네트워크 멈춤 시 안내** — 응답이 오지 않으면 스켈레톤이 계속 뛰는 상태로 남는다. 10~15초 뒤 「느립니다 · 다시 시도」로 바꿀 타이머 필요
- [ ] **터치 영역** — 기간 이동 ◀▶ 가 28px(`w-7 h-7`), 캘린더 날짜 높이 32px. 권장 44px 미달
- [ ] **태블릿 구간 1열** — 스윔레인 카드가 `lg:grid-cols-2` 라 768~1023px 에서 1열로 공간이 남는다. `md:grid-cols-2` 검토

### 🛠 운영·신뢰성

- [ ] **의견함 고도화** — 의견 상태 변경(handled)·처리 알림 / 공개 폼 Cloudflare Turnstile(봇 방지)
- [ ] **보안** — OpenRouter 키 교체(노출 이력)는 1순위 ①의 구 키 폐기와 함께 처리 / Rate Limiting 정밀화
- [ ] (선택) `GET /api/companies` 그리드 서버화 / 관리자 활동 로그
- [ ] **`/api/reports/all` 캐시 헤더** — `cache-control` 이 없어(Pages 기본 `max-age=0, must-revalidate`) 진입마다 584.9KB 를 다시 받고 다시 파싱한다
- [ ] **CLAUDE.md 「통합 검수 창구」 보완** — stg 는 주소가 하나뿐이라 **마지막에 푸시한 브랜치가 가져간다**(`deploy.yml` 이 main 푸시 때 `--branch=stg` 도 배포 + `preview.yml` 이 브랜치·PR 마다 stg 배포). 2026-09-01 에 25분 사이 stg 배포가 6건 찍혀 검수 요청이 두 번 어긋났다. 세션이 둘 이상 돌면 검수는 **배포 고정 주소**(`https://<id>.ax-biz-radar.pages.dev`, `wrangler pages deployment list` 로 확인)로 주고받는다는 한 줄 추가

---

## 참고 문서

| 문서 | 내용 |
|---|---|
| `ROADMAP.md` | 완료 이력 |
| `ARCHITECTURE.md` | 데이터 처리 전과정·저장 구조·비용·RAG 흐름 + D1 조회 쿼리 |
| `LLM-PROVIDER-GUIDE.md` | 키·모델·게이트웨이 교체 절차 (로컬 참고용, 커밋 대상 아님) |
| `프롬프트.MD` | 컨플 회차 작성 지침 |
| `CLAUDE.md` | 개발·문체·배포 지침 + 문서 역할 지도 |
| `ADMIN-GUIDE.md` | 관리자 운영 조작법 |
| `동료 가이드.md` | 하루 운영 절차 (10:00 / 17:30) |
