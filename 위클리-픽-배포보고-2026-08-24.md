# 위클리 픽 (Weekly Picks) 개발·배포 정리

> 기간 2026-08-21 ~ 2026-08-24 · 브랜치 `feat/weekly-insight` · PR #30 ~ #33 · 커밋 68건
> 이 문서는 **날짜 기준 스냅샷**이다. 유지되는 기준은 `CLAUDE.md`(규칙) · `ARCHITECTURE.md`(구조) ·
> `ADMIN-GUIDE.md`(조작법) · `TODO.md`(남은 일)에 있고, 이 문서는 그것을 갱신하지 않는다.

---

## 1. 무엇을 만들었나

한 주의 AX 동향 중 주요 이슈를 사람이 골라 **회차 단위 발행물**로 내보내는 기능이다.
기존 대시보드가 「전체를 탐색하는 화면」이라면, 위클리 픽은 「골라 놓은 것을 읽는 화면」이다.

배경은 유입 경로였다. 대시보드는 실제로는 잘 읽히지 않고, 사람들은 단톡방에 공유된 링크로 들어와
그 페이지에서 읽기를 마친다. 그래서 **링크 하나로 완결되는 페이지**와 **그 링크를 방으로 보내는 경로**를
같이 만들었다.

| 구성 | 주소 | 역할 |
|---|---|---|
| 회차 목록 | `/weekly` | 회차 커버 썸네일 그리드. 발행분이 쌓여 보이는 화면 |
| 회차 상세 | `/weekly?w=2026-W34` | 매거진 조판 발행물. 공유 링크가 가리키는 곳 |
| 뉴스레터 | `/news?w=…` | 같은 회차를 한 장씩 넘겨 보는 슬라이드. 상세 상단 썸네일로 진입 |
| 관리자 | `/admin` → 위클리 픽 | 픽 선별 · AI 초안 · 이미지 업로드 · 발행 · 메시지 발송 |
| 발송 | 웹훅 | 회차 링크를 메신저 방으로 보낸다 |

---

## 2. 진행 경과

### 2-1. 발행물 페이지 (8/21 ~ 8/22)

- 주간 발행물 페이지와 관리자 탭을 신설했다 (`097d59c`).
- 조판 후보 6종(A~F)을 프리뷰 파일로 만들어 비교한 뒤 **F안(매거진 인덱스)** 으로 확정했다 (`10a89bd`).
  카드·그림자 문법을 쓰지 않고 굵은/얇은 선과 활자 위계로만 구분하는 방식이다.
- 순서: 헤더 → 금주 한 줄 요약 → 수치 4칸 → 기업 목차 → 한컴 관점 → 금주 한눈에 → 픽 섹션 → 지난 회차.
- 같이 고친 것: `headerHTML` 이 컨테이너를 닫지 않아 픽 전체폭 다크 반전이 1,000px 로 잘리던 결함.
  발행된 회차가 없어 외부에 노출되지 않은 상태였다.

### 2-2. 회차 목록과 커버 (8/24)

- 목록을 **회차 커버 썸네일 그리드**로 전환했다 (`baac850`). 모바일 2열 · 데스크톱 3열.
- 커버는 **사진을 쓰지 않고 활자·색으로 자동 생성**한다. 이미지 저장소가 없고 매주 사진을 구하는 공수와
  저작권 판단이 늘어나는 것을 피하기 위한 선택이다. 배색 3종을 회차 번호로 순환해 구분감을 만든다.
- 카피는 고정 양식(주차 + 건수 + 키워드 3개)이라 매주 사람이 쓸 것이 없다.
- 목록에 **「발행 예정」 자리**를 두어 발행분이 한둘일 때 그리드가 비어 보이지 않게 했다 (`e83a0c1`).
  어느 주가 예정인지는 서버가 정한다. 기준은 「최신 회차 + 1주」가 아니라 **오늘이 속한 주**다 —
  한 주를 건너뛰면 최신+1 은 지난 주를 예정으로 내걸게 된다.

### 2-3. 선별 기준과 데이터 정리 (8/24)

- **주요 동향 수량 제한을 폐기**했다. 그 주에 주목할 것이 많으면 많이 싣는다. 선택은 기업별이 아니라
  **건별**이라 같은 기업의 동향이 그 주에 둘이면 따로 고른다.
- **후보 신호를 실측 기반 4종으로 교체**했다. 신규기업·새영역(가중치 2) / 공통주제·신규주제(1).
  내용 키워드 신호는 전부 90%대로 나와 판별력이 없어 폐기했다. 근거와 표는 `ARCHITECTURE.md` 10-1절.
- **기업 표기를 통합**했다. `SK Telecom`→`SK텔레콤`, `Upstage`→`업스테이지`,
  `ESTsoft`→`이스트소프트`(분류도 중견기업), `LG`→`LG AI연구원`. 「신규 기업」 신호가 오작동하던 원인이었다.
  파생 테이블과 별칭까지 정리했다.
- `Weekly Picks` 수치가 어긋나던 것을 고쳤다 — `stats.picks` 대신 실제 `payload.picks` 개수를 센다.
  발행 후 픽을 추가하면 표시 2 / 실제 6 으로 벌어졌다.

### 2-4. 픽 이미지 (8/24)

기사 사진 자동 수집을 검토한 뒤 **폐기하고 관리자 직접 업로드로 전환**했다. 확인한 것은 세 가지다.

| 검증 항목 | 결과 |
|---|---|
| `og:image` 추출·핫링크 | 기술적으로 가능(3개 매체 15건 전부 성공) |
| 우리 CSP | `img-src 'self' data:` 가 외부 도메인 이미지를 차단 |
| 해상도 | 15건 중 1건만 620px 판을 채움 (548~1,280px 분포) |

CSP 를 열어도 원본이 사라지면 지난 회차가 소급해 깨진다. 기사 사진을 내려받아 올리는 것은 링크가
아니라 복제여서 위험이 더 크다. 그래서 **관리자가 올린 파일만 싣는다**(R2 `ax-biz-radar-img`,
키는 내용 해시, `/api/pick-image?k=` 로 동일 오리진 서빙). **선택 입력**이며 올리지 않으면 활자·도형 판으로
나가 매주 의무가 되지 않는다. **출처·권리 근거는 필수**이고 비면 서버가 저장하지 않는다.

판은 12:5 로 고정한다. 원본 비율·크기 그대로 두었더니 비율이 1.33~2.55 로 제각각이라 픽이 이어지는
조판이 흐트러졌다. 잘리는 기준(위·가운데·아래)은 상세와 슬라이드 양쪽에서 쓰고, 판보다 작은 사진은
레터박스로 메운다.

### 2-5. 뉴스레터 `/news` (8/24)

- 같은 회차 데이터를 **다른 조판으로** 읽는 별개 페이지다. 이메일 발송이 아니다.
- 다크 배경 620px 고정, 가로 `scroll-snap` + 스와이프 · 클릭 · 방향키 · space · PageUp/Down · Home/End.
- 조판은 `news.js` 한 곳에서만 정하고 상세는 `window.AXNews` 로 불러 쓴다.
- 상세 상단의 표지 썸네일을 누르면 **페이지를 옮기지 않고 겹쳐 띄운다** — 읽던 자리를 잃지 않게 했다.
- 사이드바에는 노출하지 않는다. 진입점은 상세 페이지의 표지 썸네일 하나다.
- 사진이 없는 판은 활자 없이 **관계망 도형**만 둔다(모티프는 대시보드 지식 그래프).
  비주얼 판에 큰 제목을 넣지 않는 이유는 바로 아래 헤드라인과 같은 문장이라 두 번 읽히기 때문이다.

### 2-6. 메신저 발송 (8/24)

공유 텍스트 복사를 폐기하고 **웹훅 메시지 하나로 통일**했다 (`decc0e0`).

- 문구는 **서버 한 곳**(`functions/_weekly-message.js`)에서 만든다. 관리자 화면은 `dryRun` 으로 같은 함수의
  결과를 받아 보여 주므로 「보낸 것과 본 것」이 갈라지지 않는다.
- 서식은 마크다운이 아니라 Google Chat 문법이다(굵게는 별표 한 개). 마크다운 `**두 개**` 를 보내면
  별표가 그대로 보인다.
- **발행과 분리**했다. 오타를 고쳐 재발행하는 일이 흔한데 발행에 묶으면 그때마다 다시 나간다.
- 픽 제목은 자르지 않고 전문을 싣는다. 32자 + `…` 로 줄였더니 제목이 끊겨 읽혔다.
- 아웃트로는 붙이지 않는다. 자세한 것은 상세 페이지에서 읽는다.

### 2-7. 오발송 방지 — 2단계 발송 (8/24)

발송은 되돌릴 수 없다. 그래서 **눈으로 확인하는 단계를 절차가 아니라 코드로 강제**했다.

| 버튼 | 가는 곳 | 성격 |
|---|---|---|
| **[1차 리허설 발송]** | AX멘토링(테스트) | 실수해도 전사에 보이지 않음 |
| **[2차 전사 발송]** | 전사 라운지 | 되돌릴 수 없음 |

- 두 버튼 모두 **운영 화면에서** 누른다. 같은 환경이라 문구를 만드는 입력이 전부 같고, 링크 주소까지
  동일해 **1차에서 본 문구와 2차에 나갈 문구가 글자 단위로 같다.**
- 그 「같음」을 말로 두지 않고 **SHA-256 앞 16자**로 확인한다. 1차 기록에 지문을 남기고 2차에서 다시
  계산해 비교한다.
- 막는 조건 세 가지. 화면에서 막고 **서버에서 한 번 더** 막는다(화면 상태만 믿으면 새로고침으로 우회된다).

| 상황 | 결과 |
|---|---|
| 1차를 건너뛰고 2차 | `REHEARSAL_REQUIRED` — 1차를 먼저 하도록 안내 |
| 1차 뒤 회차 내용을 수정 | `TEXT_CHANGED` — 1차부터 다시 |
| stg 에서 한 1차로 운영 2차 | `REHEARSAL_OTHER_ENV` — 링크 주소가 달라 검증이 되지 않음 |

세 번째 항목은 stg 와 운영이 D1 을 공유하기 때문에 필요하다. stg 의 1차 기록이 운영 화면에도 보이는데
그 1차는 `SITE_ORIGIN` 이 달라 링크가 stg 주소다. 발송 기록에 `origin` 을 함께 적어 구분한다.

- 발송 기록은 **방별로** 적는다(`payload.notifyLog`). 회차당 한 칸이면 1차 리허설이 「이미 보냄」으로
  남아 2차에서 사실과 다른 경고가 뜬다.
- 확인창 맨 위에 **보낼 곳 이름**을 적는다. 이름표가 비면 줄을 빼지 않고 경고로 바꾼다 — 줄이 조용히
  사라지면 목적지를 확인했다고 오인한 채 누르게 된다.

---

## 3. 배포

| 단계 | 내용 |
|---|---|
| 스테이징 | 브랜치 푸시 → `preview.yml` → `stg.ax-biz-radar.pages.dev` |
| 운영 | PR #30 ~ #33 머지 → `deploy.yml` → `ax-biz-radar.pages.dev` + stg 동기화 |
| 검증 | 배포된 `/assets/js/admin.js` 를 직접 받아 크기·기능 문자열 대조 (운영·stg 65,291바이트 일치) |
| 첫 회차 | 1차 리허설로 AX멘토링 확인 → 2차 전사 라운지 발송 완료 |

운영 배포 토큰은 GitHub Secrets 에만 두어 로컬에서 운영 배포가 불가하다. 「머지 = 배포」다.

### 환경변수 (Production / Preview 각각)

| 이름 | Production | Preview(stg) |
|---|---|---|
| `WEEKLY_WEBHOOK_URL` | 전사 라운지 | AX멘토링 |
| `WEEKLY_WEBHOOK_LABEL` | `전사 라운지` | `AX멘토링(테스트)` |
| `WEEKLY_WEBHOOK_TEST_URL` | AX멘토링 | AX멘토링 |
| `WEEKLY_WEBHOOK_TEST_LABEL` | `AX멘토링(테스트)` | `AX멘토링(테스트)` |
| `SITE_ORIGIN` | `https://ax-biz-radar.pages.dev` | (비움 — 시험용임이 드러나게) |

환경변수는 **배포 시점에 묶인다.** 값을 넣은 뒤 재배포하지 않으면 런타임에 없다.
`/api/health` 가 관리자 PIN 으로 설정 여부만 `true/false` 로 보여 준다(값은 반환하지 않는다).

---

## 4. 규모

| 항목 | 값 |
|---|---|
| 기간 | 2026-08-21 ~ 2026-08-24 (4일) |
| 커밋 | 68건 (머지 제외) |
| PR | 4건 (#30 ~ #33) |
| 신규 코드 | `weekly.js` 655줄 · `news.js` 634줄 · `api/weekly.js` 715줄 · `_weekly-message.js` 90줄 · `api/pick-image.js` 72줄 |
| 신규 페이지 | `/weekly` · `/weekly?w=` · `/news` · 관리자 위클리 픽 탭 |
| 신규 저장소 | R2 `ax-biz-radar-img` (픽 이미지) |

---

## 5. 알려진 한계

| 항목 | 내용 |
|---|---|
| 웹훅 아바타·이름 | 방마다 웹훅 등록 시 각각 설정하는 값이고 우리 payload 에 없다(본문은 `text` 하나). 1차 리허설로 검증되지 않으며, 두 방의 설정 일치는 Chat 웹훅 관리 화면에서 대조할 사안이다 |
| 발송 회수 | 웹훅 메시지를 화면에서 되돌리는 기능은 없다. 지우려면 Chat 에서 스페이스 관리자 권한으로 삭제해야 한다. 2단계 발송을 둔 이유가 이것이다 |
| 카톡 링크 미리보기 | 주차별 동적 OG 이미지는 래스터화(실제 PNG 생성)가 필요해 Phase 2 로 분리했다 |
| R2 고아 객체 | 픽 이미지를 교체하면 이전 객체가 남고 정리 도구가 없다 |
| 조판 이중 관리 | `weekly-preview-news.html` 이 `news.js` 와 조판을 따로 들고 있다 |
| 프리뷰 파일 정리 | `weekly-preview-*.html` 6종은 조판 비교용이다. 첫 회차 발행이 끝나 삭제 대상이다 |

---

## 6. 다음

| 항목 | 비고 |
|---|---|
| 발행 시점 확정 | 금요일 17:30 직후 권장. 정하면 `동료 가이드.md` 에 절차 추가 |
| Vectorize 재색인 | 기업 표기 통합으로 4건의 벡터가 옛 영문 이름으로 남아 있다. `/api/ask` 검색에만 영향(323건 중 4건) |
| Phase 2 | 동적 OG 이미지, 월간판(`?m=`) |
| 정리 | 프리뷰 파일 6종 삭제, R2 고아 객체 정리 도구 |

---

## 7. 보고 메시지 (그대로 사용)

> 메신저·메일 공용. 링크는 실제 회차 주소로 바꿔 넣으면 됩니다.

---

안녕하세요. AX Biz Radar 에 **위클리 픽(Weekly Picks)** 기능을 개발해 배포했고, 첫 회차를
전사 라운지에 공유했습니다.

**무엇인가**
한 주의 AX 동향 중 주요 이슈를 선별해 회차 단위 발행물로 내보내는 기능입니다.
대시보드가 전체를 탐색하는 화면이라면, 위클리 픽은 골라 놓은 것을 읽는 화면입니다.
유입이 대시보드 방문보다 공유 링크에 집중되는 점을 고려해, **링크 하나로 읽기가 완결되는 페이지**와
그 링크를 메신저 방으로 보내는 경로를 함께 만들었습니다.

**구성**
- 회차 목록 `ax-biz-radar.pages.dev/weekly` — 회차 커버가 쌓여 보이는 그리드
- 회차 상세 `/weekly?w=…` — 공유 링크가 가리키는 발행물
- 뉴스레터 `/news?w=…` — 같은 회차를 한 장씩 넘겨 보는 슬라이드
- 관리자 화면에서 선별·발행·발송까지 한 흐름으로 처리합니다

**운영상 고려한 점**
발송은 되돌릴 수 없는 작업이라 **2단계 발송**으로 설계했습니다. 1차는 테스트 방으로 보내 실제 나갈
문구를 눈으로 확인하고, 확인 후 2차에서 전사 라운지로 보냅니다. 두 발송의 문구가 동일한지는 시스템이
지문으로 대조하며, 1차를 거치지 않거나 1차 이후 내용이 수정된 경우에는 2차가 차단됩니다.
표지 이미지는 관리자 업로드 방식이며, 올리지 않아도 활자·도형 판으로 발행되어 매주 부담이 되지 않게
했습니다(기사 사진 자동 수집은 저작권과 원본 유실 위험을 고려해 채택하지 않았습니다).

**진행 규모**
2026-08-21 ~ 08-24, 커밋 68건, PR 4건. 신규 페이지 3종과 관리자 탭 1종을 추가했습니다.

**다음 단계**
발행 시점을 금요일 17:30 직후로 정례화하는 방안을 검토 중이며, 카카오톡 링크 미리보기용 회차별
이미지 생성은 Phase 2 로 두었습니다.

상세 내역은 사내 저장소의 `위클리-픽-배포보고-2026-08-24.md` 에 정리해 두었습니다.
감사합니다.
